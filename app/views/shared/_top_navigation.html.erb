<div id="top-navigation" tabindex="-1">
  <div class="container">
    
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Exp", root_path, class: "navbar-brand" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">

            <li><%= link_to t("my_expenses"), expenses_path if user_signed_in? %></li>
            <li><%= link_to t("other_expenses"), other_expenses_path if user_signed_in? %></li>

            <li><%= link_to t("summary"), histories_path if user_signed_in? %></li>
            
            <li><%= link_to t("notes"), events_path if user_signed_in? %></li>
            <% if user_signed_in? && current_user.admin? %>
              <li><%= link_to "Admin tool", admin_root_path %></li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to "[ #{current_user.name_or_email} ]", root_path %></li>
            <% end %>
            <li><%= link_to t("sign_in"), new_user_session_path unless user_signed_in? %></li>
            <li><%= link_to t("sign_out"), destroy_user_session_path, method: :delete if user_signed_in? %></li>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

  </div>
</div>